{
  "name": "@loomhq/electron-click-through-workaround",
  "description": "mac work around for transparent click through on electron v7 and up",
  "version": "0.0.1",
  "license": "UNLICENSED",
  "gypfile": true,
  "main": "main.js",
  "types": "main.d.ts",
  "scripts": {
    "clean": "node-gyp clean",
    "build": "node-gyp build",
    "rebuild": "node-gyp clean; node-gyp configure; node-gyp build"
  },
  "repository": "https://github.com/loomhq/ElectronMacOSClickThrough",
  "dependencies": {
    "bindings": "~1.2.1",
    "node-addon-api": "^3.0.0"
  }
}
